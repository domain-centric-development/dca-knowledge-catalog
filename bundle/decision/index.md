# decision

- [Domain event or integration event: which one to raise](domain-event-vs-integration-event.md) — An event is either an **internal domain fact** or a **published cross-context contract** — and the two are different ...
- [Entity or Value Object: modelling a domain concept](entity-vs-value-object.md) — When you introduce a new domain concept, the first modelling fork is whether it is an **Entity** (has identity and a ...
- [Event delivery: synchronous, asynchronous, and when you need an outbox](event-delivery-sync-async-and-outbox.md) — Choosing how to deliver an event reliably is two independent questions, not one. Answer both, then read the row off t...
- [Pattern style per subdomain: full tactical DDD, transaction script, or buy](pattern-style-per-subdomain.md) — Not every bounded context deserves the full tactical DDD toolkit. The effort you invest should track the **subdomain ...
- [Shared kernel or duplication: where a value object lives](shared-kernel-vs-duplication.md) — When two contexts both need a concept — a `Money`, a `UserId`, an `Address` — you can promote it into the `sharedkern...

**Extensible zone — authored, not generated from sources.** `decision/` nodes are written by a human or an LLM and **survive** `generate` (only this `index.md` is regenerated). Add one as `decision/<slug>.md` with frontmatter `type: Decision`, `title:`, `tags: [decision]`, then link into the skeleton with bundle-relative links (e.g. `[UseCase](/marker/port-in/usecase.md)`).
